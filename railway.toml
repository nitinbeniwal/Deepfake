[build]
dockerfilePath = "deepfake_detector/Dockerfile"

[deploy]
startCommand   = "uvicorn api:app --host 0.0.0.0 --port $PORT"
healthcheckPath    = "/health"
healthcheckTimeout = 300
restartPolicyType  = "on_failure"

[environments.production.deploy]
numReplicas = 1
